package test

import org.jetbrains.annotations.TestOnly

class ConverterTest {

    private val delta = 0.001

    @TestOnly
    fun `meter to km`() {
//        val result = Converter.distance(1000.0, DistanceUnit.METER, DistanceUnit.KM)
//        assertEquals(1.0, result, delta)
    }

}